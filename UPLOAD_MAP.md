# Upload Map

Upload this complete folder into the first-grade GitHub repository or a subfolder such as `grade-01/`.

Open `index.html`.

Required folders: assets, data, lessons, printables, assessments, teacher-tools, records.

No CDN fonts, outside scripts, outside curriculum embeds, outside worksheet images, or required external assets.
