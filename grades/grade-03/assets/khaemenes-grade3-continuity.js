/* Khaemenes Grade 03 · Continuity Bridge v1.1.0 */
(function attachGrade3Continuity(global){
  "use strict";
  const VERSION="1.1.0";
  const LEGACY_KEY="khaemenes_grade3_subject_36_aplus_v1";
  const LEGACY_CLAIM_KEY="khaemenes_grade3_legacy_migration_claim_v1";
  const RECORDS_KEY="khaemenes_grade3_records_by_learner_v1";
  const ACTIVE_KEY="khaemenes_grade3_active_learner_v1";
  const clean=(v,m=120)=>String(v??"").replace(/[\u0000-\u001F\u007F]/g,"").trim().slice(0,m);
  const read=(k,f=null)=>{try{const r=localStorage.getItem(k);return r?JSON.parse(r):f}catch{return f}};
  const write=(k,v)=>{try{localStorage.setItem(k,JSON.stringify(v));return true}catch{return false}};
  const remove=k=>{try{localStorage.removeItem(k);return true}catch{return false}};
  function learner(){const s=global.KhaemenesElementaryContinuity?.getSummary?.();if(!s?.eligible||!s.learner||String(s.learner.grade||"").toLowerCase()!=="grade-03")return null;return Object.freeze({learnerId:clean(s.learner.learnerId),familyId:clean(s.learner.familyId)||null,nickname:clean(s.learner.nickname||"Third Grade Scholar",80),stage:"elementary",grade:"grade-03",ageBand:clean(s.learner.ageBand,40)||null,interests:Array.isArray(s.learner.interests)?s.learner.interests.slice(0,16):[],mentor:s.mentor||Object.freeze({id:"archaemenes",name:"Archaemenes",avatar:"🦉",presentationMode:"young-scholar",providedBy:"Khaemenes Academy"})});}
  function normalizeState(v={}){const x=v&&typeof v==="object"?v:{};const weekly={};for(let i=1;i<=36;i++){const n=Number(x.weekly?.[i]??x.weekly?.[String(i)]??0);if(Number.isFinite(n)&&n>0)weekly[i]=Math.max(0,Math.min(100,n));}return {student:clean(x.student||"Third Grade Scholar",80),weekly,midterm:Math.max(0,Math.min(100,Number(x.midterm||0))),final:Math.max(0,Math.min(100,Number(x.final||0))),portfolio:Boolean(x.portfolio),learnerId:clean(x.learnerId)||null,linkedAt:x.linkedAt||null,updatedAt:x.updatedAt||null,recordVersion:"4.1"};}
  function allRecords(){const v=read(RECORDS_KEY,{});return v&&typeof v==="object"?v:{}}
  function migrateLegacy(id){const records=allRecords();if(!id||records[id])return records;const raw=read(LEGACY_KEY,null);if(!raw)return records;const legacy=normalizeState(raw);const claim=read(LEGACY_CLAIM_KEY,null);const claimedId=clean(claim?.learnerId);if(claimedId&&claimedId!==id)return records;if(legacy.learnerId&&legacy.learnerId!==id)return records;const now=new Date().toISOString();records[id]={...legacy,student:learner()?.nickname||legacy.student,learnerId:id,linkedAt:legacy.linkedAt||now,updatedAt:now,migration:{source:LEGACY_KEY,mode:"single-claim-non-destructive-copy",migratedAt:now}};write(RECORDS_KEY,records);write(LEGACY_CLAIM_KEY,{learnerId:id,migratedAt:now});return records;}
  function loadState(){const l=learner();if(!l)return normalizeState({});const records=migrateLegacy(l.learnerId);const state=normalizeState(records[l.learnerId]||{});state.student=l.nickname;state.learnerId=l.learnerId;state.linkedAt=state.linkedAt||new Date().toISOString();records[l.learnerId]=state;write(RECORDS_KEY,records);write(ACTIVE_KEY,{learnerId:l.learnerId,nickname:l.nickname,activatedAt:new Date().toISOString()});return normalizeState(state);}
  function saveState(v){const l=learner();const state=normalizeState(v);if(!l)return state;state.student=l.nickname;state.learnerId=l.learnerId;state.updatedAt=new Date().toISOString();const records=allRecords();records[l.learnerId]=state;write(RECORDS_KEY,records);return normalizeState(state);}
  function clearActive(){const l=learner();if(!l)return false;const records=allRecords();delete records[l.learnerId];write(RECORDS_KEY,records);remove(ACTIVE_KEY);return true;}
  function summary(){const l=learner();const state=loadState();const vals=Object.values(state.weekly||{}).map(Number).filter(v=>Number.isFinite(v)&&v>0);const mastered=Object.values(state.weekly||{}).filter(v=>Number(v)>=80).length;const average=vals.length?Math.round(vals.reduce((a,b)=>a+b,0)/vals.length):0;let next=1;for(let i=1;i<=36;i++){if(Number(state.weekly?.[i]||0)<80){next=i;break}next=36}return Object.freeze({eligible:Boolean(l),learner:l,mentor:l?.mentor||null,state,mastered,average,next,certificateReady:Boolean(l)&&mastered===36&&Number(state.midterm)>=80&&Number(state.final)>=80&&Boolean(state.portfolio)});}
  function subscribe(fn){if(typeof fn!=="function")throw new TypeError("A listener function is required.");const emit=()=>fn(summary());const sh=e=>{if([RECORDS_KEY,LEGACY_KEY,LEGACY_CLAIM_KEY,"khaemenes_family_registry_v1","khaemenes_active_family_v1","khaemenes_active_learner_v1"].includes(e.key))emit()};global.addEventListener("storage",sh);global.addEventListener("khaemenes-family-changed",emit);global.addEventListener("khaemenes-elementary-family-ready",emit);global.addEventListener("khaemenes-naib-ready",emit);return()=>{global.removeEventListener("storage",sh);global.removeEventListener("khaemenes-family-changed",emit);global.removeEventListener("khaemenes-elementary-family-ready",emit);global.removeEventListener("khaemenes-naib-ready",emit)}}
  global.KhaemenesGrade3Continuity=Object.freeze({version:VERSION,getLearner:learner,getMentor:()=>learner()?.mentor||null,loadState,saveState,clearActive,getSummary:summary,subscribe});
})(window);